import { ParticleTextEffect } from "@/components/ui/particle-text-effect";

/** Optional full-page demo of the particle field (standalone layout). */
export default function ParticleTextDemo() {
  return <ParticleTextEffect />;
}
